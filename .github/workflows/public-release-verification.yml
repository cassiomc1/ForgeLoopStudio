name: Public release verification
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to verify'
        required: true
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Validate requested release tag shape
        shell: bash
        run: |
          TAG="${{ inputs.tag || github.event.release.tag_name }}"
          case "$TAG" in
            v[0-9]*) ;;
            *) echo "::error::tag must start with 'v' followed by a version (got: $TAG)"; exit 1 ;;
          esac
      - name: Checkout exact release tag
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ format('refs/tags/{0}', inputs.tag || github.event.release.tag_name) }}
          persist-credentials: false
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: node scripts/verify-public-release.mjs "${{ github.repository_owner }}" "${{ github.event.repository.name }}" "${{ inputs.tag || github.event.release.tag_name }}"
        env:
          GITHUB_TOKEN: ${{ github.token }}
